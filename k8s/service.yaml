apiVersion: v1
kind: Service
metadata:
  name: secure-web
  namespace: secure-web
  labels:
    app.kubernetes.io/name: secure-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: secure-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
